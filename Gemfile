source 'https://rubygems.org'

install_if -> { Gem::Version.new(RUBY_VERSION) > Gem::Version.new('2.2.1') } do
  gem 'rails', '~> 4.2'
end

# Specify your gem's dependencies in migration_data.gemspec
gemspec
